node_modules
dist
.vite
coverage

# Generated visualization bundles (rebuild with `make frontend-bundle`).
# Keep the directory tracked via .gitkeep; the UI falls back to mock data when absent.
public/bundle/*
!public/bundle/.gitkeep
